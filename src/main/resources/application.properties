# LOCAL (fallback)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/hotel_reservation_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Ashu1234@}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
server.port=${PORT}
server.address=0.0.0.0